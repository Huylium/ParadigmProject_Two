# ParadigmProject_Two
2nd project for paradigms class \ 

1. Open up SWI prolog and have location of project 2 file
2. do cd('<project 2 file location>'). without the backwards slash
3. load project 2 file by using ['MazeSolverParadigmProj', example, test].
4. test using test cases. For example: basic_map(M), display_map(M), find_exit(M, A).

